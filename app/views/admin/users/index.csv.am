csv << [ 'Created At', 'E-Mail', 'Pre-Sales', 'Marketing', 'CRM'  ]
@users.each do |user|
  csv << [
      user.created_at.in_time_zone('EST'),
      user.email,
      user.presales_opt_in,
      user.marketing_opt_in,
      user.crm_opt_in,
  ]
end
